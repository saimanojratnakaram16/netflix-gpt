import React from "react";
import { NETFLIX_LOGO } from "../utils/constants";

function Logo() {
  return (
      <img
        src={NETFLIX_LOGO}
        alt="logo"
      />
  );
}

export default Logo;
